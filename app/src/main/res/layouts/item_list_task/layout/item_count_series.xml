<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>
    </data>
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingLeft="@dimen/textSize_large"
        android:paddingRight="@dimen/textSize_large">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_centerInParent="true">
            <ImageView
                android:id="@+id/imgSerias"
                app:srcCompat="@drawable/v_counter_icon"
                android:layout_width="@dimen/spacing_normal"
                android:layout_height="@dimen/spacing_normal"/>

            <TextView
                android:id="@+id/countSeries"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/spacing_xxsmall"
                android:text="0"
                android:textAlignment="center"
                android:textSize="@dimen/textSize_normal" />
        </LinearLayout>

    </RelativeLayout>
</layout>